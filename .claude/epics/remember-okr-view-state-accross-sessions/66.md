---
name: Add ExpandedOkrIds field to Go NavigationContext structs
status: open
created: 2026-02-08T10:04:00Z
updated: 2026-02-08T10:06:47Z
github: https://github.com/rknuus/Bearing/issues/66
depends_on: []
parallel: false
conflicts_with: []
---

# Task: Add ExpandedOkrIds field to Go NavigationContext structs

## Description
Add an `ExpandedOkrIds []string` field with JSON tag `expandedOkrIds,omitempty` to the `NavigationContext` struct in all three Go locations. Update the field-by-field copy logic in the planning manager's Load/Save methods.

## Acceptance Criteria
- [ ] `NavigationContext` in `internal/access/models.go` has `ExpandedOkrIds []string \`json:"expandedOkrIds,omitempty"\``
- [ ] `NavigationContext` in `internal/managers/planning_manager.go` has the same field
- [ ] `NavigationContext` in `main.go` has the same field
- [ ] `PlanningManager.LoadNavigationContext()` copies `ExpandedOkrIds` from access context to manager context
- [ ] `PlanningManager.SaveNavigationContext()` copies `ExpandedOkrIds` from manager context to access context
- [ ] Existing `navigation_context.json` without the field loads without error (nil/empty slice)
- [ ] Go tests pass (`make test` or equivalent)

## Technical Details
- Files: `internal/access/models.go`, `internal/managers/planning_manager.go`, `main.go`
- The `omitempty` tag ensures backward compatibility — old JSON files deserialize cleanly
- The field-by-field copy in `LoadNavigationContext()` (~line 776) and `SaveNavigationContext()` (~line 789) must include `ExpandedOkrIds`

## Dependencies
- [ ] None

## Effort Estimate
- Size: XS
- Hours: 0.25
- Parallel: false (must complete before frontend tasks)

## Definition of Done
- [ ] Code implemented
- [ ] Go tests pass
