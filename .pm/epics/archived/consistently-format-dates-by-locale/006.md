---
name: Migrate EisenhowerQuadrant date display
status: open
created: 2026-02-13T16:12:10Z
updated: 2026-02-13T16:12:10Z
github: [Will be updated when synced to GitHub]
depends_on: [003]
parallel: true
conflicts_with: []
---

# Task: Migrate EisenhowerQuadrant date display

## Description
Replace the hardcoded ISO date display in `EisenhowerQuadrant.svelte` with a call to the shared `date-format.ts` utility for the "today" date shown on pending task cards.

## Acceptance Criteria
- [ ] `today` variable formatted via `formatDate()` instead of raw `toISOString().split('T')[0]`
- [ ] No raw ISO date strings displayed on quadrant task cards
- [ ] Existing CreateTaskDialog tests pass (EisenhowerQuadrant is used within CreateTaskDialog)
- [ ] Both native and browser-based app display the date correctly

## Technical Details
- Import `formatDate` from `../lib/utils/date-format`
- Change `const today = new Date().toISOString().split('T')[0]` to use formatting
- Need two values: keep ISO string for data purposes (passed to `createTask`), add formatted string for display
- e.g. `const todayIso = new Date().toISOString().split('T')[0]` and `const todayDisplay = formatDate(todayIso)`
- Replace `{today}` in template with `{todayDisplay}`
- Files affected: `frontend/src/components/EisenhowerQuadrant.svelte`

## Dependencies
- [ ] Task 003: Locale initialization wired into app startup

## Effort Estimate
- Size: XS
- Hours: 0.5
- Parallel: true (with tasks 004, 005)
