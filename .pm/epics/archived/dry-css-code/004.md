---
name: Create ErrorBanner.svelte shared component
status: open
created: 2026-02-13T14:06:13Z
updated: 2026-02-13T14:06:13Z
github: TBD
depends_on: [001]
parallel: true
conflicts_with: []
---

# Task: Create ErrorBanner.svelte shared component

## Description
Create a reusable `ErrorBanner.svelte` component in `frontend/src/lib/components/` that encapsulates the dismissable error banner pattern currently duplicated in 5+ components.

## Acceptance Criteria
- [ ] `ErrorBanner.svelte` created in `frontend/src/lib/components/`
- [ ] Props: `message` (string), `dismissable` (boolean, default `true`)
- [ ] Callback: `ondismiss`
- [ ] Renders red-tinted banner with error message and optional dismiss button
- [ ] Uses unified error colors via design tokens: `--color-error-50` bg, `--color-error-200` border, `--color-error-600` text
- [ ] Exported from `frontend/src/lib/components/index.ts`
- [ ] `make frontend-lint` and `make frontend-check` pass

## Technical Details
- Reference existing patterns from:
  - `.error-banner` in OKRView.svelte, EisenKanView.svelte
  - `.error-banner` in CreateTaskDialog.svelte
  - `.error-area` in EditTaskDialog.svelte (simpler, no dismiss)
- Normalize colors: use `--color-error-50` (#fef2f2) for bg (not #fee2e2), `--color-error-200` for border, `--color-error-600` for text
- Layout: flex row, space-between, center aligned
- Dismiss button: small × button on the right (hidden when dismissable=false)
- Margin-bottom for spacing below

## Dependencies
- Task 001 (design tokens must exist)

## Effort Estimate
- Size: XS
- Parallel: true (can be created alongside 002 and 003)
