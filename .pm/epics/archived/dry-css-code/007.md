---
name: Migrate EisenKanView, OKRView, and EisenhowerQuadrant to shared components
status: open
created: 2026-02-13T14:06:13Z
updated: 2026-02-13T14:06:13Z
github: TBD
depends_on: [002, 004]
parallel: true
conflicts_with: [008]
---

# Task: Migrate EisenKanView, OKRView, and EisenhowerQuadrant to shared components

## Description
Refactor EisenKanView.svelte, OKRView.svelte, and EisenhowerQuadrant.svelte to use shared Button and ErrorBanner components. Also extract the duplicated priority badge pattern into a shared PriorityBadge.svelte or fold it into existing components.

## Acceptance Criteria
- [ ] EisenKanView uses `<Button>` for `.create-btn` and `<ErrorBanner>` for error display
- [ ] OKRView uses `<Button>` for `.add-button`, icon buttons, and `<ErrorBanner>` for error display
- [ ] EisenhowerQuadrant and EisenKanView share priority badge via a single component (PriorityBadge.svelte or equivalent)
- [ ] Duplicated CSS for buttons, error banners, and priority badges removed from all three files
- [ ] Visual appearance unchanged
- [ ] Both Wails native and browser mock mode work correctly
- [ ] `make frontend-lint` and `make frontend-check` pass

## Technical Details
- Files to modify:
  - `frontend/src/views/EisenKanView.svelte`
  - `frontend/src/views/OKRView.svelte`
  - `frontend/src/components/EisenhowerQuadrant.svelte`
- OKRView icon buttons have many color sub-variants (edit, delete, add, save, cancel, complete, reopen, archive, nav) — use Button variant="icon" with color prop
- Priority badge is identical in EisenKanView and EisenhowerQuadrant — extract to `PriorityBadge.svelte` in `frontend/src/lib/components/`
- EisenKanView `.create-btn` is identical to `.btn-primary` — replace with `<Button variant="primary">`
- Keep view-specific layout styles (kanban columns, OKR hierarchy, quadrant grid)

## Dependencies
- Task 002 (Button.svelte)
- Task 004 (ErrorBanner.svelte)

## Effort Estimate
- Size: M
- Parallel: true (can run alongside 005, 006)
