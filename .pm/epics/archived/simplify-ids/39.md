---
name: Update Wails binding structs and conversions
status: done
created: 2026-02-07T14:30:24Z
updated: 2026-02-07T14:33:28Z
github: https://github.com/rknuus/Bearing/issues/39
depends_on: [36]
parallel: false
conflicts_with: [36]
---

# Task: Update Wails binding structs and conversions

## Description
Update the Wails binding structs in `main.go` to include `ParentID` on `Objective` and `KeyResult`, and update the conversion functions between binding types and access layer types. Then regenerate TypeScript bindings.

## Acceptance Criteria
- [ ] Binding `Objective` struct has `ParentID string` with `json:"parentId"`
- [ ] Binding `KeyResult` struct has `ParentID string` with `json:"parentId"`
- [ ] `convertObjective()` maps `ParentID` from access to binding type
- [ ] `convertObjectiveToAccess()` maps `ParentID` from binding to access type
- [ ] `wails generate` succeeds and TypeScript types include `parentId`

## Technical Details

**Files affected:**
- `main.go` — binding struct definitions and conversion functions
- `frontend/src/lib/wails/wailsjs/go/models.ts` — auto-regenerated

**Steps:**
1. Add `ParentID` to binding structs (mirror access layer changes from task #36)
2. Update `convertObjective()` to copy `ParentID`
3. Update `convertObjectiveToAccess()` to copy `ParentID`
4. Run `wails generate` to regenerate TypeScript types

## Effort Estimate
- Size: S
- Parallel: false — depends on task #36
