---
name: Migrate CreateTaskDialog and EditTaskDialog to shared components
status: open
created: 2026-02-13T14:06:13Z
updated: 2026-02-13T14:06:13Z
github: TBD
depends_on: [002, 003, 004]
parallel: true
conflicts_with: [008]
---

# Task: Migrate CreateTaskDialog and EditTaskDialog to shared components

## Description
Refactor CreateTaskDialog.svelte and EditTaskDialog.svelte to use the shared Dialog, Button, and ErrorBanner components. Remove the duplicated CSS for overlay, dialog container, buttons, and error display from both files.

## Acceptance Criteria
- [ ] CreateTaskDialog uses `<Dialog>` for overlay + container (maxWidth="700px")
- [ ] EditTaskDialog uses `<Dialog>` for overlay + container (maxWidth="400px")
- [ ] Both dialogs use `<Button variant="primary">` and `<Button variant="secondary">` for action buttons
- [ ] Both dialogs use `<ErrorBanner>` for error display
- [ ] Duplicated CSS for `.dialog-overlay`, `.dialog`, `.btn-primary`, `.btn-secondary`, `.error-banner`, `.error-area` removed
- [ ] Visual appearance unchanged (pixel-identical)
- [ ] Both Wails native and browser mock mode work correctly
- [ ] `make frontend-lint` and `make frontend-check` pass

## Technical Details
- Files to modify:
  - `frontend/src/components/CreateTaskDialog.svelte`
  - `frontend/src/components/EditTaskDialog.svelte`
- CreateTaskDialog has maxWidth 700px; EditTaskDialog has 400px — pass via Dialog prop
- CreateTaskDialog error banner is dismissable; EditTaskDialog error area is not (pass dismissable={false})
- Dialog actions slot replaces `.dialog-actions` div
- Keep component-specific styles (form layout, field groups) — only remove duplicated patterns

## Dependencies
- Task 002 (Button.svelte)
- Task 003 (Dialog.svelte)
- Task 004 (ErrorBanner.svelte)

## Effort Estimate
- Size: M
- Parallel: true (can run alongside 006, 007)
