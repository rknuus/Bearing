---
name: Refactor CreateTaskDialog to use TaskFormFields
status: open
created: 2026-02-11T19:38:44Z
updated: 2026-02-11T19:41:02Z
github: https://github.com/rknuus/Bearing/issues/99
depends_on: [98]
parallel: true
conflicts_with: []
---

# Task: Refactor CreateTaskDialog to use TaskFormFields

## Description

Replace the inline form fields in CreateTaskDialog with the shared `TaskFormFields` component. Remove duplicated form CSS. Keep the Eisenhower quadrant grid, staging logic, and dialog chrome unchanged.

## Acceptance Criteria

- [ ] CreateTaskDialog imports and uses `TaskFormFields` for the task entry form
- [ ] All 6 fields bind to existing state variables (`newTaskTitle`, `selectedThemeId`, `newTaskDescription`, `newTaskTags`, `newTaskDueDate`, `newTaskPromotionDate`)
- [ ] `disabled={isSubmitting}` passed to TaskFormFields
- [ ] Duplicated `.form-group`, `.form-row`, and input CSS removed from CreateTaskDialog
- [ ] Eisenhower grid, quadrant drag-and-drop, and "Add Task" button remain unchanged
- [ ] Visual appearance is unchanged
- [ ] Existing CreateTaskDialog tests still pass (update selectors if needed)

## Technical Details

- Replace lines 170–236 (the `<fieldset class="task-entry">` block's inner form fields) with `<TaskFormFields>`
- The `<fieldset>` wrapper and `<legend>New Task</legend>` stay in CreateTaskDialog
- The "Add Task" button stays in CreateTaskDialog (below `TaskFormFields`)
- Remove CSS: `.form-group`, `.form-group label`, `.form-group input/textarea/select`, `.form-row` — these now live in TaskFormFields

### Files affected
- `frontend/src/components/CreateTaskDialog.svelte`
- `frontend/src/components/CreateTaskDialog.test.ts` (update if selectors change)

## Effort Estimate
- Size: S
- Parallel: true (can run alongside task 003)
