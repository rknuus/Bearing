---
name: Backend data model — ThemeID to ThemeIDs
status: closed
created: 2026-03-08T08:17:00Z
updated: 2026-03-08T08:17:00Z
github: [Will be updated when synced to GitHub]
depends_on: []
parallel: true
conflicts_with: []
---

# Task: Backend data model — ThemeID to ThemeIDs

## Description

Replace `ThemeID string` with `ThemeIDs []string` on `DayFocus` across all Go backend code: the domain model, access layer, manager layer, Wails bindings in main.go, and all Go test files.

## Acceptance Criteria

- [ ] `access.DayFocus.ThemeID string` replaced with `ThemeIDs []string` (`json:"themeIds,omitempty"`)
- [ ] `main.go` Wails `DayFocus` struct updated to `ThemeIDs []string`
- [ ] `main.go` `SaveDayFocus` binding converts `ThemeIDs` correctly between Wails and access structs
- [ ] `main.go` `GetYearFocus` binding converts `ThemeIDs` correctly
- [ ] `planning_manager.go` `ClearDayFocus` sets `ThemeIDs: nil` (or empty slice) instead of `ThemeID: ""`
- [ ] All Go test files updated: `ThemeID: "X"` → `ThemeIDs: []string{"X"}`
- [ ] `make test-backend` passes
- [ ] `make lint` passes (Go linter)
- [ ] No remaining references to `ThemeID` (singular) in Go production code

## Technical Details

### Files affected
- `internal/access/models.go` — struct field rename
- `internal/access/plan_access.go` — no logic changes expected (just serializes the struct)
- `internal/managers/planning_manager.go` — `ClearDayFocus` update
- `main.go` — Wails binding struct + conversion functions
- `internal/access/plan_access_test.go` — test data updates
- `internal/managers/planning_manager_test.go` — test data updates
- `internal/integration/integration_test.go` — test data updates
- `internal/integration/benchmark_test.go` — test data updates

### Approach
- Mechanical rename: find all `ThemeID` references in DayFocus context, change to `ThemeIDs []string`
- JSON tag changes from `json:"themeId"` to `json:"themeIds,omitempty"`
- Test assertions checking `.ThemeID` become `.ThemeIDs` with slice comparisons

## Dependencies
- None

## Effort Estimate
- Size: S
- Hours: 2-3
- Parallel: true (can run in parallel with task 188)

## Definition of Done
- [ ] Code implemented
- [ ] All Go tests passing
- [ ] Go linter passes
